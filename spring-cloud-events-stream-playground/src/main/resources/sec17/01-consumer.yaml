spring:
  cloud:
    function:
      definition: consumer
    stream:
      bindings:
        consumer-in-0:
          destination: demo-topic # topic name
          group: demo-group
      kafka:
        bindings:
          consumer-in-0:
            consumer:
              ack-mode: MANUAL
        binder:
          brokers:
            - localhost:9092
          consumer-properties:
            auto.offset.reset: earliest # --from-beginning