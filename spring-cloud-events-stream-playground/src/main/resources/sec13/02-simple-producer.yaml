spring:
  cloud:
    function:
      definition: simpleProducer
    stream:
      poller:
        fixed-delay: 1000 # default value
      bindings:
        simpleProducer-out-0:
          destination: demo-topic
      kafka:
        binder:
          brokers:
            - localhost:8081