spring:
  cloud:
    function:
      definition: simpleConsumer
    stream:
      bindings:
        simpleConsumer-in-0:
          destination: demo-topic
      kafka:
        binder:
          brokers:
            - localhost:9092
          consumer-properties:
            auto.offset.reset: earliest #  --from-beginning